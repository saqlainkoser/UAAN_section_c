<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>

    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #fafafa;
            color: #333;
        }

        .container {
            display: flex;
            height: 100vh;
        }

        /* Left Navigation */
        .sidebar {
            width: 260px;
            background: #fff;
            padding: 30px;
            border-right: 1px solid #e5e5e5;
        }

        .sidebar h3 {
            margin-bottom: 20px;
            font-size: 22px;
        }

        .nav-btn {
            display: block;
            width: 100%;
            padding: 15px 18px;
            margin-bottom: 15px;
            border-radius: 10px;
            background: #e8e8e8;
            border: none;
            cursor: pointer;
            font-size: 16px;
            text-align: left;
            transition: 0.3s ease;
        }

        .nav-btn:hover {
            background: #dcdcdc;
        }

        /* Main Section */
        .main {
            flex: 1;
            padding: 40px;
        }

        .main h1 {
            margin-top: 0;
            font-size: 36px;
        }

        .overview-title {
            margin-top: 40px;
            margin-bottom: 20px;
            font-size: 26px;
        }

        /* Cards Grid */
        .cards {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
        }

        .card {
            padding: 30px;
            border-radius: 18px;
            background: #f5f5f5;
        }

        .card h3 {
            font-size: 18px;
            color: #555;
            margin-bottom: 15px;
        }

        .card .number {
            font-size: 40px;
            font-weight: bold;
        }

        /* Card Colors */
        .c1 { background: #eef2ff; }
        .c2 { background: #eefbea; }
        .c3 { background: #fff3e6; }
        .c4 { background: #f6efff; }
    </style>
</head>

<body>

<div class="container">

    <!-- Sidebar -->
    <div class="sidebar">
        <h3>Navigation</h3>

        <button class="nav-btn">Departments</button>
        <button class="nav-btn">Users</button>
        <button class="nav-btn">Add User</button>
        <button class="nav-btn"><a href="/create-department">Add Department</a></button>
    </div>

    <!-- Main -->
    <div class="main">
        <h1>Admin Dashboard</h1>

        <h2 class="overview-title">Dashboard Overview</h2>

        <div class="cards">

            <div class="card c1">
                <h3>Total Departments</h3>
                <div class="number">totalDepartments </div>
            </div>

            <div class="card c2">
                <h3>Total Students</h3>
                <div class="number"> totalStudents</div>
            </div>

            <div class="card c3">
                <h3>Total Professors</h3>
                <div class="number"> totalProfessors </div>
            </div>

            <div class="card c4">
                <h3>Total HODs</h3>
                <div class="number"> totalHods </div>
            </div>

        </div>
    </div>

</div>

</body>
</html>
